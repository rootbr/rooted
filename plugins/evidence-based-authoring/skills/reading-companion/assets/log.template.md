# Reading log — *{{BOOK_TITLE}}*

Fleeting notes: the reader's own thoughts, dictated while reading. Captured **verbatim, in his words** — not paraphrased, not "improved" (the value is in his formulation; the generation effect requires that he produce it). Only obvious speech-to-text artifacts are cleaned.

This is raw input. In a separate pass these entries are distilled into artifacts (cards / rules / scripts) — see `../CLAUDE.md` → "Note-taking workflow".

Format:
- `## YYYY-MM-DD` per reading session.
- `### §<section>` subheading when the section changes.
- `- HH:MM — <verbatim thought>` per dictated note.

---
