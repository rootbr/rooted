## Brightness calibration

At the bottom of the menu sits a status line "Brightness: now N · peak M" showing the current brightness.

In Auto mode the profile is ACEScg, and brightness is computed from the Sun's elevation above the horizon.
